Create a candidate worldbuilding asset for the current work project.

If the asset type is `world`, produce core world rules, constraints, power sources, social order, taboos, history pressure, and open questions.

If the asset type is `location`, produce a location that affects choices, access, sensory texture, and conflict.

If the asset type is `organization`, produce public goals, hidden goals, resources, conflicts, and limits.

Output must be valid JSON for the named schema. Include `risks` and `source_paths`.
