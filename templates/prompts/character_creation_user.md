Create a candidate character-related asset for the current work project.

If the asset type is `character`, produce a full character profile with BDI, psychology, background story, speech style, arc, and state.

If the asset type is `background-story`, produce a hidden backstory that influences behavior but should not appear directly in prose.

If the asset type is `relationship`, produce relationship graph facts as candidates only.

Output must be valid JSON for the named schema. Include `risks` and `source_paths`.
