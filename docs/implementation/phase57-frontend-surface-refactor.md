# Phase 57: Frontend Surface Refactor

本阶段把本地前端从“工程控制台”收束为三个用户入口：

- 创作总监
- 文风学习
- 全局配置

设定工坊、工作流运行、项目管理等低层入口不再直接暴露给用户。底层能力仍保留在后端，作为创作总监 agent loop 的内部工具使用。

目标是让用户只通过自然对话和少量必要配置驱动创作系统，避免被候选资产、workflow mode、内部审查命令和文件路径打断。
