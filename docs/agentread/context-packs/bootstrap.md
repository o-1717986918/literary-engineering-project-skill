# Bootstrap Context Pack

用于新 Agent 快速进入项目：

- 项目目标：工程级长篇虚构文学创作系统。
- 源状态：canon、characters、plot、style、memory、reviews。
- 主要 CLI：`init`、`index`、`context`、`simulate-scene`、`draft-scene`、`review-scene`、`run-workflow`、`run-langgraph`、`serve-api`。
- 约束：不绕过审查，不直接写 canon，不把检索结果当事实。
