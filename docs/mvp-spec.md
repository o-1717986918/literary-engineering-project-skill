# MVP 规格

MVP 目标：让一部原创作品拥有可初始化、可检索、可推演、可审查、可导出的工程结构。

必备能力：

- 初始化作品项目。
- 建立轻量记忆索引。
- 生成场景上下文包。
- 编译公版或授权风格 profile。
- 生成角色推演工作台。
- 生成草稿工作台和审查报告。
- 汇总章节、审计长篇、导出多格式产物。
- 用 `run-workflow`、`run-langgraph`、`serve-api` 接入编排层。
